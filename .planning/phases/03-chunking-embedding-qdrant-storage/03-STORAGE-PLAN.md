---
phase: 3
plan: 3
name: qdrant-storage-upsert
wave: 2
depends_on: [core-chunking-strategies, embedder-integration]
requirements: [PROC-06]
files_modified: [internal/rag/store.go, cmd/server/main.go]
autonomous: true
---

# Plan 03: Qdrant Storage Upsert

Implement the storage layer for vectors and metadata in Qdrant, including model consistency validation.

## Tasks

<task id="3-03-01" requirements="PROC-06">
<action>
Create `internal/rag/store.go` with `Store` interface.
`Store` should have `Upsert(ctx context.Context, docID string, chunks []string, vectors [][]float32, strategy string) error`.
Add an implementation `QdrantStore` using `github.com/qdrant/go-client/qdrant`.
Include payload mapping ( `doc_id`, `chunk_index`, `text`, `strategy`).
</action>
<read_first>
- internal/rag/store.go
- .planning/phases/03-chunking-embedding-qdrant-storage/03-CONTEXT.md
</read_first>
<acceptance_criteria>
- `internal/rag/store.go` exists and defines `Store` interface.
- `Upsert` method creates points with correct collection name.
- Payload includes required fields.
- `go test ./internal/rag` (mocked) passes.
</acceptance_criteria>
</task>

<task id="3-03-02" requirements="PROC-06">
<action>
Implement consistency check in `QdrantStore.Init`.
Check `embedding_model` in collection metadata/params.
If model mismatch (`nvidia/llama-nemotron-embed-vl-1b-v2:free` vs existing), return error on startup.
</action>
<read_first>
- internal/rag/client.go
</read_first>
<acceptance_criteria>
- `QdrantStore.Init()` checks for model consistency.
- `go test -v ./internal/rag -run TestStoreConsistency` passes.
</acceptance_criteria>
</task>

## Verification Criteria

<must_haves>
- UUID is converted into a valid Qdrant point ID.
- Payloads are indexed for future filtering.
- Collection creation uses 2048 dimensions and cosine similarity.
- System fails if existing data was generated by a different model.
</must_haves>

<automated>
- `go test -v ./internal/rag -run TestStore`
</automated>
