# Plan: Phase 02-02 PDF & DOCX Parsers

Implement specialized text extraction for PDF and DOCX files using 3rd-party libraries.

## Context
- **Phase:** 02 - Document Parser
- **Goal:** Extract text from proprietary document formats.
- **Decisions:** Use `ledongthuc/pdf` for PDF; `nguyenthenguyen/docx` for DOCX.

## Requirements
- [ ] **PROC-01**: Parse text from PDF and DOCX files.
- [ ] **PROC-02**: Explicit error message for scanned PDFs.

## Proposed Changes

### Dependencies
- [ ] Run `go get github.com/ledongthuc/pdf`
- [ ] Run `go get github.com/nguyenthenguyen/docx`

### internal/rag
- [ ] Implement `PDFParser` in `parser.go` using `ledongthuc/pdf`.
- [ ] Implement `DOCXParser` in `parser.go` using `nguyenthenguyen/docx`.
- [ ] Implement `TXTParser` in `parser.go` with UTF-8 cleaning.
- [ ] Implement `MDParser` in `parser.go` using `goldmark` with table support.
- [ ] Implement `Validator` in `parser.go` for length and language checks.

## Verification Plan

### Automated Tests
- [ ] Unit test `PDFParser`: Handle a sample multi-page PDF.
- [ ] Unit test `PDFParser`: Verify error returned for an image-only (scanned) PDF.
- [ ] Unit test `DOCXParser`: Verify extraction from a standard `.docx` with bold/italic text and lists.

### Manual Verification
- [ ] Run a test tool to parse a real-world financial `.pdf` (text-based) and ensure meaningful output.
- [ ] Run a test tool to parse a standard `.docx` and confirm output matches expected text content.
