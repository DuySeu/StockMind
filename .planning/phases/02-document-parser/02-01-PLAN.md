# Plan: Phase 02-01 Document Parser Core & Simple Formats

Implement the foundational `Parser` interface and the implementations for TXT and Markdown files, including specialized handling for Markdown structure.

## Context
- **Phase:** 02 - Document Parser
- **Goal:** Backend text extraction and quality validation.
- **Decisions:** Use `abadojack/whatlanggo` for language check; `yuin/goldmark` for MD structure.

## Requirements
- [ ] **PROC-01**: Parse text from MD and TXT files.
- [ ] **PROC-02**: Validate text quality (language, min length).

## Proposed Changes

### Dependencies
- [ ] Run `go get github.com/abadojack/whatlanggo`
- [ ] Run `go get github.com/yuin/goldmark`

### internal/rag
- [ ] Create `parser.go` defining the `Parser` interface and common helper functions.
- [ ] Create `text.go` implementing `TXTParser` with UTF-8 cleanup.
- [ ] Create `markdown.go` implementing `MDParser` using `goldmark` with a custom renderer to preserve table rows and link text.
- [ ] Create `validator.go` defining the `Validator` to check for language (VI/EN) and minimum 100 characters.

## Verification Plan

### Automated Tests
- [ ] Unit test `MDParser`: Verify table structure is semi-preserved in text.
- [ ] Unit test `MDParser`: Verify link text is retained.
- [ ] Unit test `TXTParser`: Verify UTF-8 normalization.
- [ ] Unit test `Validator`: Verify rejection of non-VI/EN content or too-short strings.

### Manual Verification
- [ ] Run a small script to parse a sample `.md` with tables and print the output to console.
